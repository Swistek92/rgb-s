import React, { Component } from "react";

export default class ColorCreator extends Component {
  constructor(public color: string) {
    super(color);
  }

  render() {
    return <div>ColorCreator</div>;
  }
}
